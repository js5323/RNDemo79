buildscript {
    ext {
        buildToolsVersion = "34.0.0"
        minSdkVersion = 24
        compileSdkVersion = 34
        targetSdkVersion = 34
        ndkVersion = "27.1.12297006"
        kotlinVersion = "2.0.21"
        mpaas_baseline = '10.2.3-62'
        mpaas_artifact = 'mpaas-baseline'
    }
    repositories {
        mavenLocal()
        maven { url 'https://maven.aliyun.com/repository/central' }
        maven { url 'https://maven.aliyun.com/repository/public' }
        maven { url 'https://maven.aliyun.com/repository/gradle-plugin' }
        maven { url 'https://maven.aliyun.com/repository/apache-snapshots' }
        maven { url 'https://maven.aliyun.com/nexus/content/groups/public/' }
        maven {
            credentials {
                username "mvn_read_ws"
                password "mrk8929"
            }
            url "https://mvn.cloud.alipay.com/nexus/content/repositories/releases/"
        }
        maven {
            credentials {
                username "626668353c6b5273a0ce984d"
                password "t[4AU)JOBzXN"
            }
            url "https://packages.aliyun.com/63dc774a9dee9309492b993a/maven/repo-jpkdy/"
        }
        mavenCentral()
        google()
    }
    dependencies {
        classpath("com.android.tools.build:gradle")  // 建议使用兼容 34 的 Gradle 版本
        classpath("com.facebook.react:react-native-gradle-plugin")
        classpath("org.jetbrains.kotlin:kotlin-gradle-plugin")
        classpath 'com.android.boost.easyconfig:easyconfig:2.8.2'
    }
}

configurations {
  all*.exclude group:'com.android.support', module: 'support-compat'
}

allprojects {
    repositories {
        flatDir {
            dirs 'libs'
        }
        mavenLocal()
        maven { url 'https://maven.aliyun.com/repository/central' }
        maven { url 'https://maven.aliyun.com/repository/public' }
        maven { url 'https://maven.aliyun.com/repository/gradle-plugin' }
        maven { url 'https://maven.aliyun.com/repository/apache-snapshots' }
        maven {
            credentials {
                username "mvn_read_ws"
                password "mrk8929"
            }
            url "https://mvn.cloud.alipay.com/nexus/content/repositories/releases/"
        }
        maven { url 'https://maven.aliyun.com/nexus/content/groups/public/' }
        maven {
            credentials {
                username "626668353c6b5273a0ce984d"
                password "t[4AU)JOBzXN"
            }
            url "https://packages.aliyun.com/63dc774a9dee9309492b993a/maven/repo-jpkdy/"
        }
        google()
        mavenCentral()
    }
}

apply plugin: "com.facebook.react.rootproject"
